import networkx as netx
import numpy as np
import pygame as pyg
import scipy


class roads:
    def __init__(verticies, edges, sys_type='small world'):
        self.veriticies=veritices if vertiices else 2
        self.vertitcies=edges 
        if (edges > verticies-1):
            raise ValueError:
                print("Graph must have at least V-1 edges")
        elif (edges < scipy.misc.comb(verticies, 2))
            and  edges < 








class town: 
    def __init__(self, size, population):
        self.size='medium'
        self.population=200
        
        
    